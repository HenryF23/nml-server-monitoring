global:
  scrape_interval: 15s
  scrape_timeout: 10s
  evaluation_interval: 15s

# Monitored servers push through the remote-write receiver. Run node/setup-node.sh
# on this host as well when the central server itself should be monitored.
scrape_configs: []
